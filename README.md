# dotfiles

Personal configuration files for macOS, managed with GNU Stow.

## Usage

From the repo root:

```bash
stow ghostty
```

Remove symlinks:

```bash
stow -D ghostty
```

Preview changes:

```bash
stow -n -v ghostty
```
